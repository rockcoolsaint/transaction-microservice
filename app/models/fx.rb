class Fx < ApplicationRecord
  # def self.output_amount(input_amount, currency)
  #   if currency != "naira"
  #     input_amount * where(name: currency).rate
  #   else
  #     input_amount / where(name: currency).rate
  #   end
  # end
end
